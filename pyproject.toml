[project]
name = "simplequiz"
version = "0.1.0"
description = "A dead simple quiz webapp."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.4",
    "django-cors-headers>=4.9.0",
    "djangorestframework>=3.17.1",
    "psycopg2-binary>=2.9.11",
]
